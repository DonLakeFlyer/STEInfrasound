name: CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["**"]

jobs:
  syntax-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install system dependencies
        run: |
          sudo apt-get update -y
          sudo apt-get install -y libportaudio2 libasound2-dev

      - name: Install Python dependencies
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt

      - name: Syntax check
        run: python3 -m py_compile ste_infrasound.py infrasound.py infrasound_core.py infrasound_ui.py infrasound_bar.py infrasound_detect.py infrasound_detections.py

      - name: Run tests
        run: python3 -m pytest -q
